# Backend origin (no trailing slash). Used for both REST API and Socket.io.
# Local dev can omit this — the app falls back to <current-host>:3000.
# Production (Vercel): set this to your Render backend URL.
VITE_API_URL=https://your-backend.onrender.com
